package parkingLotV2.entities.vehicle;

public class Car extends Vehicle {
    public Car(String licensePlate, parkingLotV2.enums.VehicleType vehicleType) {
        super(licensePlate, vehicleType);
    }
}
